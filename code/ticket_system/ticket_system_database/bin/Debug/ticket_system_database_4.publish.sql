/*
Deployment script for ticket_system

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "ticket_system"
:setvar DefaultFilePrefix "ticket_system"
:setvar DefaultDataPath "C:\Users\Jgain\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"
:setvar DefaultLogPath "C:\Users\Jgain\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[group].[manager_id] on table [dbo].[group] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[group])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Starting rebuilding table [dbo].[group]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_group] (
    [g_id]          INT           NOT NULL,
    [manager_id]    INT           NOT NULL,
    [g_name]        VARCHAR (MAX) NOT NULL,
    [g_description] VARCHAR (MAX) NOT NULL,
    PRIMARY KEY CLUSTERED ([g_id] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[group])
    BEGIN
        INSERT INTO [dbo].[tmp_ms_xx_group] ([g_id], [g_name], [g_description])
        SELECT   [g_id],
                 [g_name],
                 [g_description]
        FROM     [dbo].[group]
        ORDER BY [g_id] ASC;
    END

DROP TABLE [dbo].[group];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_group]', N'group';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Starting rebuilding table [dbo].[group_member]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_group_member] (
    [g_id] INT NOT NULL,
    [e_id] INT NOT NULL,
    CONSTRAINT [tmp_ms_xx_constraint_PK_group_member1] PRIMARY KEY CLUSTERED ([g_id] ASC, [e_id] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[group_member])
    BEGIN
        INSERT INTO [dbo].[tmp_ms_xx_group_member] ([g_id], [e_id])
        SELECT   [g_id],
                 [e_id]
        FROM     [dbo].[group_member]
        ORDER BY [g_id] ASC, [e_id] ASC;
    END

DROP TABLE [dbo].[group_member];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_group_member]', N'group_member';

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_constraint_PK_group_member1]', N'PK_group_member', N'OBJECT';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Creating Table [dbo].[users]...';


GO
CREATE TABLE [dbo].[users] (
    [Id]       INT            IDENTITY (1, 1) NOT NULL,
    [userId]   NVARCHAR (50)  NOT NULL,
    [username] NVARCHAR (MAX) NOT NULL,
    [password] NVARCHAR (MAX) NOT NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC),
    CONSTRAINT [AK_user_id_key] UNIQUE NONCLUSTERED ([userId] ASC)
);


GO
PRINT N'Update complete.';


GO
